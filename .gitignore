# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# typescript build cache (must NOT be committed — breaks Railway/Nixpacks builds)
*.tsbuildinfo

# production
/build

# local file storage (uploaded PDFs/photos live on the Railway volume, not in git)
/storage

# env / secrets — NEVER commit these
.env
.env*.local
.env.local

# misc
.DS_Store
*.pem
npm-debug.log*
/coverage
.vercel
